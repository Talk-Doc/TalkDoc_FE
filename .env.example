# 백엔드 API 주소.
# 비워두면 같은 origin(/api, /ws)으로 요청하며, 개발 서버(vite)는 이를 VITE_DEV_BACKEND_URL로 프록시합니다.
# 배포 시 프론트와 백엔드 도메인이 다르면 여기에 백엔드 주소를 넣으세요. (예: https://api.talkdoc.example)
VITE_API_BASE_URL=

# vite dev 서버가 프록시할 백엔드 주소 (개발 전용)
VITE_DEV_BACKEND_URL=http://localhost:8080
